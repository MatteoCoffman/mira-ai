{
  "name": "mira-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:lambda": "bash ../scripts/build_lambda_bundle.sh",
    "cdk": "cdk",
    "deploy": "npm run build:lambda && cdk deploy",
    "synth": "cdk synth"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-cdk": "^2.175.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.175.0",
    "constructs": "^10.4.2",
    "dotenv": "^16.4.7"
  }
}
